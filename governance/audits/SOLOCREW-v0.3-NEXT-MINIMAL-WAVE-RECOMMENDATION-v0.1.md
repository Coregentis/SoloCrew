# SOLOCREW v0.3 Next Minimal Wave Recommendation v0.1

## Purpose

This note recommends the smallest next bounded implementation wave, grounded only in current `main` truth.

## Recommended Next Wave

Recommended next wave:

- **one bounded console-side correction/apply wave over existing sealed `v0.1` truth**

Suggested focus:

- connect the current single-cell operator console line to the already existing bounded correction/writeback helper in `projection/assembly/flow-assembly.ts`
- keep the interaction single-cell only
- keep the interaction operator-facing only
- keep the writeback path limited to already authorized `v0.1` correction/writeback truth

## Why This Is The Highest-Leverage Remaining Gap

This is the highest-leverage remaining gap because it would turn the current console from:

- a truthful scaffold-heavy operator surface

into:

- an honestly usable bounded operator console

without requiring:

- provider execution
- channel integrations
- multi-cell behavior
- Secretary behavior
- broad KPI cockpit breadth
- new runtime law

The current repo already has:

- page rendering
- bootstrap composition
- continuity/reload presentation
- delivery and work visibility
- correction/review targeting and draft surfaces
- a sealed bounded correction/writeback helper

So the remaining leverage is in connecting those truths, not inventing new ones.

## Recommended Wave Scope

The minimal next wave should do only this:

1. expose one bounded operator-authored correction/apply path from the current console line
2. reuse current target-scope, review-intent, and draft truth where appropriate
3. call the existing bounded correction/writeback assembly path rather than inventing a new runtime law
4. reassemble and surface updated memory-summary, review-strip, and continuity-facing truth honestly
5. add focused tests for same-session apply behavior and fresh sqlite reload truth after the console-side correction path

## Explicitly Rejected For The Next Wave

The next wave should explicitly reject:

- provider-backed request submission
- request execution engines
- channel entry or channel handoff
- portfolio or Secretary routing
- persistent operator workflow timelines
- multi-step approval engines
- broad business-pack execution breadth
- broad KPI cockpit expansion
- any new `Cognitive_OS` runtime law invented inside SoloCrew

## Recommendation Conclusion

The smallest credible next step is not another visibility scaffold.

It is one narrow operator-usable apply path that reuses already sealed correction/writeback truth.

If that wave lands cleanly and keeps all deferred/non-claims honest, `v0.3-single-cell-usable` should be in position for a realistic seal decision immediately afterward.
