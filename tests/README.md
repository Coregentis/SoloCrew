# Tests

Executable tests are intentionally deferred for this baseline.

This repository currently freezes contracts and skeletons only; downstream tests should begin once projection mappings and adapters gain runnable entrypoints.
